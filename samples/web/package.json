{
  "name": "@trinsic/connect-web-sample",
  "version": "1.0.0",
  "description": "Small sample showcasing different interaction modes of Trinsic's library",
  "main": "index.js",
  "scripts": {
    "build": "npm run-script copy-library",
    "copy-library": "cd ../../client/web && npm install && npm run build && cp dist/index.* ../../samples/web/dist/connect-web && cd ../../samples/web",
    "prestart": "npm run-script copy-library",
    "start": "live-server --cross"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "live-server": "1.2.2"
  }
}
